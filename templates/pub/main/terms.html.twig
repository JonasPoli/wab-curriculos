{% extends 'pub/base.html.twig' %}
{% block title %}Termos de Uso{% if tenant %} · {{ tenant.name }}{% endif %}{% endblock %}

{% block body %}
<div style="max-width:800px; margin: 2rem auto; padding: 0 1.5rem;">
    <div class="card" style="padding: 2.5rem;">
        <h1 style="font-family:'Plus Jakarta Sans',sans-serif; font-size:2rem; font-weight:800; margin-bottom:1.5rem; color:var(--text)">Termos de Uso</h1>
        <div class="terms-content" style="line-height:1.7; color:var(--text2)">
            {% if tenant and tenant.lgpdTermsHtml %}
                {{ tenant.lgpdTermsHtml|raw }}
            {% else %}
                <p>Nenhum termo de uso cadastrado para este portal.</p>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
